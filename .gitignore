# Dependencies (build is dependency-free, but keep these out just in case)
node_modules/
npm-debug.log*

# Private / unsanitized case studies — keep locally, never publish
_private-raw-case-studies/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
