# Phase 0 — knowledge layer + retrieval
fastembed==0.4.2
chromadb==0.5.23

# Phase 1 — grounded chat API
fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2
pydantic==2.9.2
python-dotenv==1.0.1

# Offline embedding fallback (CI / air-gapped smoke tests)
scikit-learn>=1.3
